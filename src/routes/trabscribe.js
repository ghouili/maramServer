const express = require('express')
const TranscribeCOntroller = require('../controllers/trabscribe')

const route = express.Router()


// route.get('/', TranscribeCOntroller.Transcribe);

module.exports = route